- Prompt : 
le Controller s'occupe de mettre en lien le View le Moteur de jeu. où dois je mettre la logique de boucle principale du jeu, ainsi que la fonction simulate_round?

- Réponse : 
Boucle principale (lecture input etc) dans game_controller
Game_controller appelle simulation.step

- Contenu pris : tout; même si changements plus tard

Création des tests principalement initialisée avec l'IA puis modification/ajouts des tests pour qu'il soient plus précis/adaptés.

Les classes abstraites sont décidées par nous, mais l'implémentations de toutes les fonctions qui implémentent les fontions de la classe abstraite est souvent dans un premier temps fait via l'IA dans un premier temps puis modifié pour être plus adapté par nos soin au code si besoin.  Ce sont souvent des fonctions bêtes.

L'IA a beaucoup aidé pour le JS/front-end